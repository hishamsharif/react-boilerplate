import styled from 'styled-components';

const StyledListView = styled.div.attrs({
  className: 'container',
})`
  border-bottom: 1px solid grey;
  white-space: nowrap;
  padding-left: 25px;
  padding-right: 25px;
`;

export default StyledListView;
