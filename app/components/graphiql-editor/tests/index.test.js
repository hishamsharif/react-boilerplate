// import React from 'react';
// import { shallow } from 'enzyme';
// import GraphiQL from 'graphiql';
// import GraphiQLEditor from '../index';

describe('<GraphiQLEditor />', () => {
  it('should render the codemirror editor', () => {
    // const renderedComponent = shallow(<GraphiQLEditor />);
    // expect(renderedComponent.find('div').length).not.toBe(0);
  });
});
